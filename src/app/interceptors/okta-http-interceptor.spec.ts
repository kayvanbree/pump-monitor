import { OktaHttpInterceptor } from './okta-http-interceptor';

describe('OktaHttpInterceptor', () => {
  it('should create an instance', () => {
    expect(new OktaHttpInterceptor()).toBeTruthy();
  });
});
